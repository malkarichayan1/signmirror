export default function Footer() {
  return (
    <footer className="landing-footer">
      <div className="landing-container">
        <span>SignMirror — practice ASL with your own camera.</span>
        <span>Built with MediaPipe hand tracking. Runs entirely in your browser.</span>
      </div>
    </footer>
  );
}
